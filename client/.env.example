# Deployed backend origin, used at build time to point the client at the API.
# Leave blank for local dev — the app falls back to "/api" and the Vite proxy
# forwards to http://localhost:3000.
# Production example: VITE_API_URL=https://campuslens-api.onrender.com
VITE_API_URL=
